public with sharing class NewTestClass {
    public NewTestClass() {

/* This is a new test class created just now will merge this local/dev
with the Patch branch and merge it with the Master branch then deploy this to
org. */
System.debug('This is a new test class');
    }
}
